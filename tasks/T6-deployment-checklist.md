# T6: デプロイ運用チェックリストの整備

- ステータス: todo
- 依存: なし

## 背景
初回デプロイで実際に起きた事故: APP_BASE_URL未反映→ログイン後にローカルへ飛ぶ／OPENAI_API_KEYの改行混入→全LLM呼び出し死亡（定型文ナレーション化）。いずれも「環境変数はRedeployまで反映されない」「キーの改行」という運用知識で防げた。READMEにトラブル表は追加済みだが、**変更時に機械的に辿れるチェックリスト**が未整備。

## 目的
デプロイ・環境変数変更・Spotify設定変更のたびに辿る手順を1枚に固定し、事故の再発をゼロにする。

## 触ってよいファイル
- 新設 `DEPLOY_CHECKLIST.md`
- `README.md` `HANDOFF.md`（リンク追記）
- 任意: 環境変数の存在・形式を起動時に検証して分かりやすく警告するコード（`src/server/` 内。挙動を変えない範囲）

## 触らないほうがよいファイル
- 認証・fill等の本体ロジック

## 実装方針
チェックリストに含める: 環境変数4点の存在と形式（APP_BASE_URLの一致・httpsか・末尾スラッシュなし、キーに改行/空白がないか）→ Redeploy実行 → ログイン一周 → 1曲再生 → `/api/log?limit=5` でsource確認 → Vercel Logsに赤がないか。Spotify側: Redirect URI二本・User Management。ロールバック手順（VercelのRollbackボタン）も明記。

## 完了条件
- 新しい人が DEPLOY_CHECKLIST.md だけを見て再デプロイ・検証・切り戻しまでできる
- 過去2事故がチェック項目として明示されている

## 人間が確認すること
- 実際に1回チェックリストを辿ってみて、詰まらないか

## 動作確認手順
- チェックリスト自体のドライラン（本番Redeploy1回）

## 中断時の引き継ぎ欄
（着手時に記入）
