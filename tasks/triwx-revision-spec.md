# TRIWX 選曲品質改修 指示書

作成日: 2026-07-13(最終更新: 2026-07-19)
根拠: 再生ログ150セグメント(11セッション、人間評価41件: good 26 / ok 28 / bad 15)の分析

---

## 現在地(まずここだけ読めばいい)

**次にやる1つだけ選ぶなら → 「L5種曲フォールバックに地域カード照合を追加」。理由: `africa + grit + texture=100`の実運用テストで、AI生成が5連続却下となりL5が発火、無関係なGrover Washington Jr.(ジャズ)が選出される実害が実際に出た。以前は「既知の穴・フォローアップ」扱いだったが、実害確認により最優先に格上げ。**

### ✅ 実現済み(2026-07-19時点でデプロイ・確認済み)
- 変更1: midnight/morningの情景ベース定義(実運用でbadRate改善を確認)
- 変更2: gritの境界拡張(THE PILLOWS等でgood確認)
- 変更6: 自己判定内蔵の生成プロンプト(`promptVersion: v9-self-judge`で稼働中)
- 変更8: judgeRejections(却下理由配列)は既にログに記録されている(model名の記録は要確認)
- 変更9(カード・スライダー全面見直し): 12枚+12枚+4スライダー、実運用で確認済み
- **変更12・14・15-2・15-3(国籍宣言の検証、ハードコード補正リスト、逆接分割): 本番デプロイ済み。複数回の実運用テストを経て安定を確認。The Strokes、Martin Garrix、Tame Impala、Moses Sumney等を正しく却下できている。都度追加の運用は継続中。**
- **変更13・15-1(grit×textureの動的差し替え): 本番デプロイ済み。実運用テストで、Massive Attack、Ladytron、alt-J、Portishead、Jamie xx等の電子的なUKアーティストが安定して選出されることを確認。当初の「ギター一辺倒」問題は実質解決。**
- **変更15-4(バージョン文字列フィルタにMixパターンを追加): 本番デプロイ済み。`'s Mix`+`Radio/Club/Extended/Dub Mix`を追加し、「Mixed Emotions」「Original Mix」等を誤爆しないことを回帰テストで確認済み。実運用での再テストは未実施。**
- **`grit + africa + texture=100`の検証(2026-07-19実施)**: `KNOWN_ARTIST_NATIONALITY_OVERRIDES`に`"lyra pramuk": "usa"`を追加(実際はアメリカ・ペンシルベニア出身、ベルリン拠点なのに「南アフリカ」と虚偽申告し選出された)。検証スクリプトで補正が機能することを確認済み。同テストで判明した以下は記録のみ・対応不要と判断:
  - **Akonの括弧書きによる国籍宣言のすり抜け**: 「国籍/活動拠点: アメリカ（セネガル系アメリカ人アーティスト）」のように、主張は正しく「アメリカ」だが括弧内の地域名にregionMatchesの部分文字列一致がヒットし誤って合格していた(変更15-2の逆接語分割の対象外)。境界線上のケース(出生地と民族的ルーツが両方実在)であり、記号を追いかけ始めるとキリがないため対応見送り、既知のパターンとして記録のみ。
  - **gqom(DJ Lag)等、地域固有の電子音楽ジャンルへの自己判定の誤り**: 南アフリカ発の生々しい電子音楽ジャンルであるgqomが「エレクトロニックな質感が足りず」と事実に反する理由で却下された。モデルの自己判定の質の問題でコード側では対応不可、記録のみ。
  - **Ayub Ogada「Kothbiro」の選曲理由の事実誤認**: 実際は繊細なアコースティック曲なのに「ハーシュノイズ系のテクニックが感じられる」と選出理由に書かれていた。国籍とは無関係な、選曲理由そのものの正直さの問題でコード側では検出不可、記録のみ。
- T4: 散らし具合レポート(`/api/log?stats=1`)、動作確認済み
- ジングルの声の固定ファイル化(ただしテキストは要再修正、下記11-1)

### ❌ 試したが効果なしと判明したもの
- **変更13の初期案(gritの定義文にエレクトロニックな例を追記するだけの対策)**: 効果なしと判明。動的差し替え(15-1)に切り替えて解決した。

### 🔲 未着手(優先順位順、上から着手推奨)
**A. 選曲ロジック(影響大きい順)**
1. **L5種曲フォールバックに地域カード照合を追加**: `africa + grit + texture=100`で実際に発火し、無関係なGrover Washington Jr.(アメリカのジャズ)が選出される実害を確認(2026-07-19)。最優先に格上げ。
2. **`grit + india`/`grit + latin`の検証**: `africa`は検証済み(上記)。india/latinは未検証のまま。
3. **`grit + dance`の検証**: 踊れる指定がgritの引力でパンク/ガレージに流れないか。
4. 国籍虚偽申告のハードコード補正リストへの追加運用継続(新顔が見つかり次第、都度追加)
5. resolveMetaのtitleMismatch対策(19%と高い。変更7a/7b。15-4のMixフィルタと合わせて対応)
6. `lang_ja+dance`のcomboHint追加(goodRate 0%で確定)
7. replayのカード照合(変更7c)
8. カード選択上限のバグ修正(シーンdrive×focus×walkの排他を追加)
9. nostalgiaカードの削除(決定済み・未実装)

### ✅ 検証したが優先度が低いと判明したもの
- **`grit + heat=0(クール)`**: textureほど深刻な矛盾ではないと確認(2026-07-19実測)。パンク/ガレージロックは実際に"抑制されたボーカル"と形容しうる曲柄が多く、heat=0との衝突は軽微。当面は様子見でよい。

### ⚠️ 教訓(次回以降も忘れないこと)
1. **コードを直したら、必ずコミット→push→Vercelの新しいデプロイ完了を確認してからテストする。** 2026-07-19の作業で、この確認を怠ったために古いビルドに対するテスト結果を「直っていない」と誤認する時間を要した。
2. **プロンプトへの文言追加には限界がある。** 「〜してはいけない」「〜を優先せよ」という指示を足すだけでは、モデルの学習データに根付いた強い言葉の連想(grit→ギター、地域×ジャンルの結びつき等)を覆せないことがある。文言強化を1〜2回試して効果が薄ければ、コード側のハード検証や動的な定義差し替えに切り替える判断を早めにする(実際にgrit×textureはこの切り替えで解決した)。
3. **モグラ叩きに見えても、毎回違う層が見つかっている。** 国籍偽装は「書かない→偽る→正直に書いた上で言い訳する→逆接語を使わず堂々と偽る」と、対策のたびに違う抜け道が見つかったが、これは同じバグの再発ではなく、掘るたびに新しい深さに到達している証拠。完璧な根絶より、見つかるたびに都度対応する運用で十分。
4. **仕様書(spec)はコードとの整合性が命。** ローカル(実装者)とチャット(議論の整理役)の両方が同じファイルを触ると、重複や書き漏れが起きやすい。どちらが最後に更新したかを都度明確にする。実例(2026-07-19): チャット側は変更15/16/17として3つの独立した変更番号をコミットメッセージに使っていたが、ローカル側は同じ内容を変更15の下位項目(15-1〜15-4)として整理していた。コードコメントと本ファイルの番号を15-1〜15-4に統一した(過去のコミットメッセージ自体は履歴なので変更していない)。

**B. 音声・体験(選曲ロジックとは別PR推奨)**
7. 局名コールのテキスト短縮(TRIW STATION→TRIWのみ、11-1)
8. ナレーション・時報の話速を上げる(11-2, 11-3)
9. 時報の合図音の見直し(11-2)
10. ナレーション自体の内容改善(変更10、要調査)

以下は上記の根拠となった分析の詳細。着手時に該当箇所だけ読めば十分です。

---

## 背景(要約)

- bad 15件の主因は3系統: (a) 時間カードの定義がアレンジベースで情景とずれる(midnight事故9件)、(b) 3枚使い時に浮いた1枚が黙って無視される(grit不足3件)、(c) パイプライン起因(replayのカード無照合、Spotify解決のバージョン事故)。
- 数値ゲート(judge)の却下回数と人間評価は無相関(bad率: 却下0回=25%、1-2回=21%、3回以上=22%)。ゲートは形式条件しか見ておらず、情景適合を判定していない。
- LLM審査段は追加しない(レイテンシ優先)。生成プロンプト内部の自己判定で代替する。
- 設計目標: **選曲の幅を狭めずにbadを減らす**。嵌めるのは情景・感情、広げるのはジャンル・国・年代。

---

## 変更1: 時間カードのpromptTextを情景ベースに書き換え

対象: `src/pipeline/definitions.ts` の moodCards

原則: 時間カードは「いつ・どこ(情景)」だけを主張し、音像・BPM・熱量には言及しない。それらは moodカードと heatスライダーの管轄(直交化)。

### midnight(最優先)

```
夜更けの情景を持つ曲。夜の街、ネオン、月、終電後、ひとりの部屋、バーの片隅——歌詞・タイトル・曲の世界観が「夜」を描いていることが第一条件。アレンジの静かさは条件ではない。静かでも昼や野外の情景の曲(牧歌的フォーク、青春讃歌、朝の歌)は不可。夜の情景があれば、音数が多い曲やグルーヴのある曲でもよい。
```

較正例(定義に含めるか、生成プロンプト側の【判定の較正例】に置く):
- 適合: ブルー・ライト・ヨコハマ(静かではないが夜の情景)、黄昏のビギン
- 不適合: 風をあつめて(静かだが昼の情景)、翼をください・青い山脈(青春讃歌)

### morning

```
朝の情景を持つ曲。目覚め、朝の光、始まり、出かける前の時間——世界観が「朝」または「一日の始まり」を描いていること。夜の情景・深夜の空気を持つ曲は、音が軽やかでも不可。重厚・壮大すぎる曲は朝に合わない。
```

較正例: HONNE「Crying Over You」は音がメロウでも夜の曲なので不適合。Arcade Fire「Crown of Love」は壮大すぎて不適合。

### dusk / afternoon

同じ方針で書き換え(dusk: 昼と夜の境目の情景・感傷、afternoon: 午後のゆるんだ時間の情景)。音像への言及は「補助条件にすぎない」と明記するか削る。rain / sunny_holiday は現状維持でよいが、余裕があれば同様に。

## 変更2: grit の定義境界を拡張

ユーザーのgood評価メモに基づき、gritの適合範囲を「歪み」より広く定義する:

```
ざらついた質感。歪んだギター、荒い録音、生々しいエネルギー。歪みがなくても、録音の生々しさや非ポップスな手触り(ローファイ、ガレージ的なラフさ、オルタナのカバー等)があれば適合。クリーンに整音された美しいフォークロックの名曲(例: Harvest Moon的なもの)は、名曲でも不適合。ジャンルの指定ではなく質感の軸であり、ブルースの生々しさ、初期ヒップホップの粗さ、メタルの轟音など、ジャンルを横断する。
```

例示を意図的に離れたジャンルから採ることで、例示が「柵」ではなく「軸の見本」として働くようにする(選曲の幅を守るため)。

## 変更3: カード排他の追加

definitions.ts に追加。UIの選択制御とプロンプト側検証の両方がこれを参照する(単一ソース)。

```typescript
// 排他定義
export const exclusionGroups: string[][] = [
  ["morning", "afternoon", "dusk", "midnight", "sunny_holiday"], // 時間帯はグループ内排他
];

export const exclusionPairs: [string, string][] = [
  ["doze", "dance"],
  ["doze", "grit"],
  ["doze", "uplift"],
];

export function conflictsWith(id: string, selected: string[]): string[] {
  const out = new Set<string>();
  for (const g of exclusionGroups) {
    if (g.includes(id)) selected.filter(s => s !== id && g.includes(s)).forEach(s => out.add(s));
  }
  for (const [a, b] of exclusionPairs) {
    if (a === id && selected.includes(b)) out.add(b);
    if (b === id && selected.includes(a)) out.add(a);
  }
  return [...out];
}
```

- rain は時間帯と共存しうる(雨の朝)ため排他グループに含めない。
- UI挙動: 後から選んだカードが勝ち、衝突する既選択カードは自動で外れる(押したら差し替わる)方式。
- 注: `midnight + dance` は排他にしない(ログでok 4 / bad 0。化学反応として活かす → 変更4)。

## 変更4: comboHints(化学反応する組み合わせの追加解釈)

網羅しない。LLMの自然解釈が外しそうな組だけ少数登録し、テストで「ヒント不要」とわかったら削る運用。

```typescript
export const comboHints: { cards: string[]; promptText: string }[] = [
  {
    cards: ["midnight", "dance"],
    promptText: "深夜のクラブ・フロアの時間帯。ディープハウス、テクノ、夜が深まるほど効くグルーヴ。昼のパーティーチューンではなく、暗さと快楽が同居する曲。",
  },
  {
    cards: ["bittersweet", "dance"],
    promptText: "泣きながら踊る音楽。切なさと踊れるビートの同居(Robyn的な様式、本来のディスコが持っていた感情)。切ないミッドテンポに丸めない。",
  },
  {
    cards: ["rain", "dance"],
    promptText: "雨の都市のダンスミュージック。UKガラージ、ダブステップ、しっとりした質感とビートの共存。",
  },
  {
    cards: ["morning", "bittersweet"],
    promptText: "徹夜明けの朝、昨夜を引きずった朝。清潔な朝ではなく、カムダウンの時間。",
  },
  {
    // 実装済み(変更14で追加・デプロイ済み、2026-07-19)
    cards: ["grit", "uk"],
    promptText: "イギリスのガレージロック/インディーリバイバル。Arctic Monkeys、The Libertines、Kaiser Chiefs、Franz Ferdinand(スコットランド)、The Vaccinesなど。同時代のアメリカのガレージロックリバイバル(The Strokes、The White Stripes、The Killersなど)と音楽的に近いが、これらはアメリカのバンドなので選ばない。",
  },
  {
    // 未実装(9-10で決定、現在地の未着手リストに掲載)
    cards: ["lang_ja", "dance"],
    promptText: "日本語の踊れる曲。和モノファンク、渋谷系のクラブ・ハウス、シティポップ・リバイバルのダンサブルな曲調など。量産的なアイドルポップのダンスナンバーは避ける。",
  },
];
```

上記のうち`grit+uk`は変更14で実装済み。`lang_ja+dance`は9-10で決定済みだが未実装(現在地の未着手リスト参照)。この一覧が全comboHintsの単一ソースであり、各変更セクション(9-10、変更14等)内のコードスニペットは決定の経緯を示す参考であって、実装時はこの一覧を正とする。

describeState でカード集合が cards をすべて含む場合、【組み合わせの解釈】セクションとして出力に追加する。

## 変更5: describeState に優先順位・正直さセクションを追加

describeState の出力末尾に固定セクションを追加:

```
【条件が両立しないとき】
- 優先順位は「ことば・地域 > 雰囲気カード > スライダー」。
- 適合曲が見つからない場合、人気度→年代の順でスライダー条件を緩めてよい。雰囲気カードは緩めない。
- 条件を緩めた場合は、選曲理由にどの条件を緩めたかを明記する。
- 選曲理由には、その曲が実際に持つ特徴だけを書く。条件に合わない曲を合うと偽って正当化しない。
```

popularity「定番」バンド(81-100)のpromptText末尾にも追記: 「ただし雰囲気カードとの適合が最優先。適合する定番曲が尽きたら、知名度を下げてでも雰囲気を守る。」

## 変更6: 生成プロンプトの骨格を変更(自己判定の内蔵)

選曲プロンプトを以下の構造にする。5候補→自己判定→1曲は同一コール内で完結させる(追加レイテンシはトークン分のみ)。

```
あなたはラジオDJとして次の曲を選ぶ。

【いまの卓の条件】
{describeState() の出力}
{comboHints 該当時は【組み合わせの解釈】}

【選曲の原則】
1. 雰囲気カードは「情景・感情」の指定であり、ジャンルの指定ではない。
   条件を満たすなら、どのジャンル・国・年代からでも選んでよい。
   定義文の例示はジャンルの範囲ではなく、軸がジャンルを横断することを示す見本である。
2. 条件が両立しないときは【条件が両立しないとき】の優先順位に従う。
3. 直近の流れと同じアーティスト・同じ質感が続いたら、条件の範囲内で意図的に離れた場所から選ぶ。

【判定の較正例】
「静かな曲」と「夜更けの曲」は別物である。風をあつめて(はっぴいえんど)は静かだが昼の情景であり midnight に不適。ブルー・ライト・ヨコハマは静かではないが夜の情景であり適合。判定は音の静かさではなく情景で行う。

【手順】
まず候補を5曲挙げる。次に各候補を、雰囲気カードの一枚ずつすべてに照らして自己判定する——その曲が実際に持つ情景・性格に基づいて。全カードを満たす曲だけを残す。「合うと言えなくもない」程度の曲は落とす。よく知らない曲は、落とさずに理由へ「推測」と明記して残してよい(無名曲は発見の源泉のため排除しない)。最も確かな1曲を選び、実際の特徴だけを理由として出力する。
```

重要な設計判断:
- 「各カードごとに照合、全カード合格」を明文化する(3枚使い時の「浮いた1枚の黙殺」対策。bad 3件の直接原因)。
- 知らない曲は「落とす」ではなく「推測と明記して通す」(選曲の幅を守る。bad 1件の残存リスクは許容)。

## 変更7: コード側フィルタ(数値ゲートの同類、レイテンシほぼゼロ)

### 7a. バージョン文字列フィルタ

Spotify解決結果のトラック名に以下を含む場合は弾き、別バージョンを再解決:
`カラオケ / Karaoke / Instrumental / インストゥルメンタル / Live / ライブ / ライヴ / Remix / リミックス / cover(自アーティスト以外) / Acoustic Version` 等。
(実績: 「青い山脈 - オリジナル・カラオケ」がjudge通過してbadになった)

### 7b. 年ズレ検知

解決結果のリリース年と、選曲AIが想定した年(または既知の原曲年)が大きくずれる場合(目安: ±10年超)、別リリース(オリジナルアルバム優先)を再解決。
(実績: 異邦人が2024年盤=パイプオルガン版、ルビーの指環が2011年盤=ピアノ版を掴んでbad)

### 7c. replayのカード照合

replay(再注入)前に、対象曲の元のconditionSnapshotと現在のカード集合を照合。少なくとも地域カード(lang_ja等)が不一致なら注入しない。可能なら雰囲気カードの主軸も照合。
(実績: lang_ja指定中にreplay経由で非日本語曲が7件混入、うちbad 1件)

## 変更8: ログの拡充(検証インフラ)

再テストの効果測定のために、セグメントログへ以下を追加:

- `model`: 選曲に使ったモデル名(現状未記録。プロンプト効果とモデル効果の切り分けに必須)
- `promptVersion`: プロンプトのバージョンタグ
- `judgeRejections`: 却下の回数だけでなく `{track, reason}` の配列(却下理由の分析用)
- 生成コール内の自己判定で落ちた候補と理由(可能なら。5候補→1曲の脱落ログは定義改良の材料になる)

## 既知の別件(今回のスコープ外、余裕があれば)

- eraバンドの隙間: 2000〜2004年がどのバンドの明示レンジにも入らない(〜1975 / 1970-1999 / 2005〜 / 2018〜)。意図的でなければ 1970-2004 に修正。
- nostalgia の基準世代が未定義(「誰の世代か」)。テストで違和感が出たら基準年代を明示するか era と連動させる。
- experimental × 深掘りは「実験音楽の大定番」に収束しがち。必要なら「実験音楽の教科書的名盤は避ける」を追記。

## 再テスト計画

1. 改修後、**同一条件での再テスト**を先に行う: `midnight + lang_ja`(era=0, popularity=100)と `dusk + nostalgia + grit`(era=52)。bad率の差分を見る。
2. 次に同方向3枚(morning+grit+uplift型)をもう1〜2組。
3. 判断に自信のない組み合わせを少数——採点データとしては弱いが、「採点せずに聴いて楽しいか」を測る(こちらが本来の目的)。

## 机上検証の結果(参考)

bad 15件のうち、本指示書の対策での防止見込み:
- 情景定義+較正例: 5件(RADWIMPS、BEGIN、花の首飾り、風をあつめて、翼をください)
- 各カード個別照合: 4件(Crown of Love、Summer Breeze、青い影、Harvest Moon)
- morning情景化: 1件(HONNE)
- コード側: 4件(カラオケ=文字列、Silk City=replay照合、異邦人・ルビーの指環=年ズレ)
- 残存リスク許容: 1件(風の行方=無名曲の知識不足。幅を守るため「推測明記で通す」を選択)

good 26件への誤爆チェック済み。夜に駆ける・地上の星・ブルー・ライト・ヨコハマは新定義(情景ベース)で生存。grit境界例(Beirut、The Watson Twins)は変更2の境界拡張で生存。

---

## 変更14: 国籍虚偽申告が高頻度・系統的に発生している問題(2026-07-19 実運用フィードバック)

### 背景
変更12(国籍宣言のハード検証)デプロイ・検証後の追加テストで、`uk + grit`(texture=100含む)条件を複数セグメント再生したところ、**7セグメント中4件でアメリカのバンドが「国籍/活動拠点: イギリス」と虚偽申告し、そのまま選出された**:

- The Killers(アメリカ・ラスベガス)→「イギリス」と申告
- Sir Sly(アメリカ・カリフォルニア)→「イギリス」と申告
- The Strokes(アメリカ・ニューヨーク)→「イギリス」と申告
- The White Stripes(アメリカ)→「イギリス」と申告

一方、却下ログ(judgeRejections)では同じセッション内でRats(The Walkmen)やI Wanna Be Your Dog(The Stooges)を正しく「国籍/活動拠点: アメリカ」と却下できている。つまり**却下時は正確な知識を持っているのに、選出候補になった対象に対してだけ虚偽の申告をする**という、前回(変更12)確認したThe Killers単体の事例が、実は稀な例外ではなく**系統的なパターン**であることが判明した。

### 原因分析
虚偽申告された4組はいずれも**2000年代のガレージロック/インディーロック・リバイバル**という同一ジャンルに属する。このジャンルは歴史的に米英両方から同時多発的に生まれた特殊な経緯を持つ(米: The Strokes, The White Stripes / 英: The Libertines, Kaiser Chiefs, Franz Ferdinand等)。`grit`カードが「ガレージロック的な質感」を強く連想させ、モデルが**「ガレージロック的な音=イギリス的」という誤った一般化**をしている可能性が高い。ランダムな知識不足ではなく、**ジャンルと国籍を混同する系統的なクセ**とみられる。

### 対策(2段構え)

**14-1. 頻出誤認アーティストのハードコード補正リスト**
モデルの自己申告より優先して機械的に上書きする、軽量な補正リスト。今回判明した4組を含む、ガレージロック/インディーリバイバル界隈の頻出アーティストを対象に、実際の国籍を固定的に持つ。

```typescript
// モデルが誤認しやすい頻出アーティストの国籍補正リスト
// (自己申告より優先してハード上書きする。ジャンル×国籍の混同が
//  系統的に起きるカテゴリから優先的に追加)
const KNOWN_ARTIST_NATIONALITY_OVERRIDES: Record<string, string> = {
  "the killers": "usa",
  "sir sly": "usa",
  "the strokes": "usa",
  "the white stripes": "usa",
  // 以降、実運用で誤認が確認され次第、追加していく運用とする
};
```

`verifyRegionDeclaration`の前段で、アーティスト名(正規化済み)がこのリストに存在すれば、モデルの申告を無視してこの値を国籍として採用する。リストは実運用で見つかった誤認のみ都度追加する運用とし、網羅的なデータベース構築は行わない(コスト対効果を優先)。

**14-2. `grit + uk`のcomboHintで正しい実例を提示**
単純な禁止ではなく、正しい実例を与えてモデルの誤った一般化を上書きする。

```typescript
{
  cards: ["grit", "uk"],
  promptText: "イギリスのガレージロック/インディーリバイバル。Arctic Monkeys、
  The Libertines、Kaiser Chiefs、Franz Ferdinand(スコットランド)、
  The Vaccinesなど。同時代のアメリカのガレージロックリバイバル
  (The Strokes、The White Stripes、The Killersなど)と音楽的に近いが、
  これらはアメリカのバンドなので選ばない。",
},
```

### 再テスト計画
実装後、再び`uk + grit`(texture=100含む)で複数セグメント再生し、以下を確認する:
- ハードコード補正リストに含まれるアーティストが誤って選出された場合、正しく弾かれるか
- comboHintの実例提示により、そもそも虚偽申告自体の頻度が下がるか(前回7件中4件からの改善幅)
- リストにない新たな誤認パターン(別ジャンル・別国籍の組み合わせ)が出ないか、引き続き監視する

### 汎用的な注意点(他の組み合わせでも同型の事故が起きうる)
今回の原因は「カードが特定ジャンルを強く連想させ、かつそのジャンルが複数国にまたがって存在する」という条件が揃ったときに起きた。同じ構造は他の組み合わせでも起こりうる:
- `grit + usa`(逆パターン。イギリスのガレージ/パンクバンドがアメリカと誤認される可能性)
- `dance + uk`/`dance + europe`(エレクトロダンスミュージックは英独仏蘭等にまたがる巨大シーン)
- `experimental + uk`/`experimental + usa`(クラウトロックは独、ノーウェイヴは米だが影響関係が複雑)
- 隣接する地域カード間の混同(`europe`と`uk`、`asia`と`kr`等)

これらを先回りして全部潰そうとはせず、変更14と同じ運用方針(ハードコード補正リストは実運用で誤認が実際に見つかったものだけ都度追加する)を維持する。網羅的な事前対応はコストに見合わない。



## 変更13: grit定義がtexture(エレクトロニック)を上書きしてしまう問題(2026-07-19 実運用フィードバック)

### 背景
`grit + uk`条件でtextureをエレクトロニック側(100)に設定してテスト再生したところ、選出された曲はMogwai・The Clash・Arctic Monkeys・Muse・Harry Styles等、**全曲ギター中心のロック/パンク系**で、エレクトロニックなプロダクションの曲は一つも出なかった。textureスライダーの指定が実質無視されている。

### 原因(推定)
gritの定義文(変更2)の例示が「歪んだギター」「ガレージ」「ブルース」「メタルの轟音」とロック/生楽器寄りの語彙に偏っており、エレクトロニックな"ざらつき"(インダストリアル、ビットクラッシュ、ハーシュノイズ、荒いブレイクビーツ等)への言及が一切ない。モデルが"ざらつき"を反射的にギターと結びつけ、texture=エレクトロニックの指定より例示の重力を優先してしまっている。

grit(質感の粗さ)とtexture(生楽器⇄電子)は本来直交する軸であり、矛盾しない(例: インダストリアル、EBM、ハーシュノイズ系テクノは"電子的でざらついた"音楽として実在する)。設計意図としては両立可能なはずが、定義文の偏りで片方が事実上死んでいる状態。

### 対策(実施済みだが効果なしと判明): gritの定義文にエレクトロニックな例を追加

```
grit(改訂版、2026-07-19実装・デプロイ済み): "ざらついた質感。歪んだギター、荒い録音、生々しいエネルギー。
歪みがなくても、録音の生々しさや非ポップスな手触り(ローファイ、ガレージ的な
ラフさ、オルタナのカバー等)があれば適合。クリーンに整音された美しい
フォークロックの名曲(例: Harvest Moon的なもの)は、名曲でも不適合。
ジャンルの指定ではなく質感の軸であり、ブルースの生々しさ、初期ヒップホップの
粗さ、メタルの轟音、インダストリアルの歪んだシンセ、ビットクラッシュされた
電子ドラム、ハーシュノイズ系テクノの荒さなど、生楽器・電子楽器を問わず
ジャンルを横断する。textureがエレクトロニック側に振られている場合、
ギターではなく電子的な歪み・粗さの側面でgritを満たす曲を選ぶこと。"
```

### 実運用テスト結果(2026-07-19): 効果なし
本番デプロイ済み(git merge-base --is-ancestorで確認、Vercelデプロイも変更13・14を含むことを確認済み)の状態で`grit + uk + texture=100`を再テストしたが、**Arctic Monkeys、Blur、The Kooks、The Smiths、The Ting Tings等、依然として全曲ギター中心のロック**。policySnapshotには「**エレクトロニックな要素は避け、生楽器主体の粗さを追求します**」と、texture=100の指定と真逆の方針が明記された。

**結論: プロンプト文言への追記(電子的な例を足す)だけでは、gritの"ギター"という連想を覆せない。** これは変更12(国籍)で「文言追加→効果薄い→コード側ハード検証」という流れを辿ったのと同じ壁。「ざらつき」という言葉自体が、モデルの学習データの中でロック/ギタージャンルの語りと圧倒的に強く結びついており、定義文をどう書いても完全には覆せない可能性が高い。

### 次の一手: textureが極端な値のとき、gritの定義自体をコード側で動的に差し替える(次回実装)

文言強化(prompt内に追記)では覆せなかったので、**gritのpromptText自体を、texture値に応じてコード側で条件分岐させる**設計に切り替える。単一の固定文字列ではなく、生成関数にする。

```typescript
// definitions.ts
// gritはtexture値に応じて説明文の重心を動的に変える。
// 通常の(電子例を後方に添えるだけの)固定文言では、ギターへの言及が
// 先に来ることで連想が強く残ってしまうため(2026-07-19実測で確認済み)、
// texture極端値のときは電子的な説明を「主」、ギター的な説明を「例外」として
// 前後を入れ替える。
function getGritPromptText(textureValue: number): string {
  if (textureValue >= 80) {
    // エレクトロニック優勢: 電子的な粗さを主役にし、ギターは終盤に触れるのみ
    return "ざらついた質感。インダストリアルの歪んだシンセ、ビットクラッシュ" +
      "された電子ドラム、ハーシュノイズ系テクノの荒さ、荒いブレイクビーツなど、" +
      "電子的な歪み・粗さを最優先で探すこと。歪んだギターのロック/パンクは" +
      "この条件ではむしろ避ける(textureがエレクトロニック指定のため)。" +
      "クリーンに整音されたポップなシンセ(テクノポップの名曲等)は、" +
      "名曲でも不適合。ジャンルの指定ではなく質感の軸である。";
  }
  if (textureValue <= 20) {
    // アコースティック優勢: 従来通りギター/生楽器中心の説明(変更2のまま)
    return GRIT_ACOUSTIC_PROMPT_TEXT; // 既存の変更2定義文をそのまま使用
  }
  // 中間値: 両論併記(現行の変更13定義文をそのまま使用)
  return GRIT_NEUTRAL_PROMPT_TEXT;
}
```

`describeState`および`generateCandidates`の両方で、gritカードのpromptTextを参照する箇所を、この関数の呼び出しに置き換える(単一ソース設計を維持: 呼び出し元を増やさず、関数を1箇所に集約する)。

ポイントは、**「電子的な例を後から付け足す」のではなく、「電子優勢のときは電子的な説明を主役にして、ギターの話を意図的に後回しにする、あるいは明示的に除外する」**という発想の転換。今回の実測(文言追加だけでは効果なし)を踏まえ、"言葉の位置と量"そのものを動的に操作する。

### 再テスト計画
実装後、以下を確認する:
- `grit + uk + texture=100`: 電子的な質感の曲(Nine Inch Nails, Author & Punisher, 荒いテクノ/インダストリアル系)が出るか
- `grit + uk + texture=0`: 従来通りギターロック(Arctic Monkeys等)が出続けるか(退行がないか)
- `grit + texture=50(中間)`: 両方の要素が状況に応じて出てもよい(固定した偏りがないか)

### 参考: heat=0(クール)との比較(2026-07-19実測)
同時に`grit + uk + heat=0(クール)`もテストしたところ、こちらは`texture`ほど深刻な矛盾ではないと判明した。選出された曲(The Undertones、The Last Shadow Puppets、Two Door Cinema Club等)は、パンク/ガレージロックの中でも実際に「抑制されたボーカル」と形容しうる曲柄で、heat=0の指定とgritの「生々しいエネルギー」がそこまで激しく衝突していない。**heat×gritはtextureほど優先度が高くないと判断し、当面は様子見でよい。**

### 参考: 国籍虚偽申告は新顔が継続中(2026-07-19実測)
同じテストセッションで、ハードコード補正リストに未登録の新たな虚偽申告が2件発生した:
- The Hives(スウェーデン)は正しく国籍を自己申告したが、却下理由に「イギリスでの影響力が高いため」という、禁止したはずの言い回しが残っていた(実害なし、判定自体は正しく却下)
- **Killah Priest(アメリカ)とRage Against the Machine(アメリカ)が「国籍/活動拠点: イギリス」と虚偽申告**。RATMは実際に`status: played`まで進んでしまった。

ハードコード補正リストは機能しているが(The Strokesは3回とも正しく却下)、**リストにない新しいアーティストが継続的に見つかる、いたちごっこの状態**。都度追加の運用方針通り、次回リストに"rage against the machine": "usa", "killah priest": "usa"を追加する。

### 汎用的な注意点: 同じ構造の罠が他にもありそうな組み合わせ(2026-07-19追記、未検証)
今回の問題の本質は「gritという言葉自体がギター/ロックへの強い連想を持ち、それに反する指定(texture=エレクトロニック)を上書きしてしまう」こと。同じ構造は他の組み合わせでも起こりうる:

- **`grit + heat=0(クール)`**: heatのクール端は「感情表現が抑制された、抑えたボーカル」で、gritの「生々しいエネルギー」と同型の綱引きになりうる(実際にはシューゲイザー、スロウコア等"クールでざらついた"音楽は存在する)。textureと対になる、**未検証の最優先確認事項**。
- **`grit + 非英米圏の地域カード`(india, africa, latin等)**: gritがそもそも英米ギターロックに強く引っ張られる性質を持つなら、`grit + india`のような組み合わせで、指定地域固有の"ざらつき"の伝統(デザートブルース、アフロビートの生々しさ、クンビア・ビジェラ等)を無視し、英米風ガレージロックに寄せてしまう可能性がある。国籍偽装(変更14)とは異なる種類のズレ(地域は正しいが音楽性がその地域らしくない)。未検証。
- **`grit + dance`**: 踊れる曲を求めているのに、gritの引力でパンク/ガレージ寄りの"踊れないロック"に流れる可能性。comboHintsに未収録、要確認。

## 変更15: gritの動的差し替え(実装完了)、逆接分割による国籍判定バグ修正、バージョン文字列フィルタ拡充(2026-07-19)

### 15-1. gritのpromptTextをtexture値に応じて動的に差し替え: 実装・デプロイ・実運用確認済み

変更13の「次の一手」として設計した動的差し替えを実装。コミット済み・Vercel本番デプロイ済み。

**実運用テスト結果(良好)**: `grit + uk + texture=100`で、Massive Attack、Ladytron、alt-J、David Bowie、Portishead、Jamie xxなど、電子的な質感を持つ本物のUKアーティストが選出されるようになった。policySnapshotも「インダストリアルやハーシュノイズ系テクノ、荒いブレイクビーツを中心に」と一貫しており、以前(Arctic Monkeys、Blur等の純ギターロックしか出なかった状態)からの改善が複数セッションにわたって確認できている。**変更13の問題は実質的に解決したとみてよい。**

### 15-2. 国籍判定の新しいバグ発見と修正: 逆接語による正当化のすり抜け

**発見された問題**: Travis Scott(アメリカ)が`uk`条件で選出。candidateWhyは以下の通り:
> 「国籍/活動拠点: アメリカ**だが**、イギリスで数回パフォーマンスを行っており、UKガラージに影響を与えたアーティストとしての評価がある」

モデルは正しく「アメリカ」と自己申告していたが、直後に禁止したはずの"影響力"型の正当化を続けて記述。コード側で実際に検証したところ、`verifyRegionDeclaration`が抽出した宣言文字列は「アメリカだが、イギリスで数回パフォーマンスを行っており、UKガラージに影響を与えた...」という文章全体であり、`regionMatches`がこの中に含まれる「イギリス」「UK」を単純な部分文字列一致(`.includes()`)で検出し、誤って合格判定(`true`)を返していた。

これは変更12改訂(虚偽申告)とは異なる**コード側のロジックバグ**——モデルは正直に申告していたのに、判定側が申告文の「言い訳」部分に釣られていた。

副次的な観察として、同じテストでjudgeRejectionsに「Firestarter(The Prodigy)」「Chokehold(Bring Me The Horizon)」が**「イギリスのアーティストであるため不合格」**という、地域カードの意図と逆方向の理由で自己却下される現象も見られた(実害はなし)。モデルの自己判定ロジックが混乱している徴候として記録。

**対策(実装・デプロイ済み)**: `verifyRegionDeclaration`に`HEDGE_SPLIT_RE = /だが|けど|しかし|ただし|もっとも/`を追加し、宣言文をこの逆接語で分割して最初に断言した部分だけを判定するよう修正。

回帰テスト(8ケース、APIを叩かない検証スクリプト)で全て合格を確認済み: The Strokes(記載なし→却下)、正しいUK宣言(合格)、The Killers(虚偽申告→ハードコード補正リスト側の対象なので`regionMatches`単体は`true`のまま、という既存の挙動を壊していないことも確認)、usa/world_tripの判定など、既存の挙動への影響なし。

**実運用再テスト結果**: Martin Garrix(オランダ)、Tame Impala(オーストラリア)、Brandy & Monica(記載なし)を正しく却下できることを確認。Travis Scott型の「正直な申告+言い訳」パターンは解消された。

**既知の限界**: 逆接語リストは今回判明した5語(だが/けど/しかし/ただし/もっとも)のみ。「とはいえ」「ではあるものの」等、リストにない逆接表現で同型のすり抜けが再発する可能性がある。都度追加の運用とする。

### 15-3. 国籍虚偽申告の新顔: Moses Sumney(逆接語を使わない直接的な虚偽申告)

15-2の修正後も、逆接語を使わない新しいパターンの虚偽申告が発生:
> Moses Sumney(実際はアメリカ、カリフォルニア出身・ガーナ育ち・LA/アシュビル拠点)が「エレクトロニックな要素とざらつきがあり、**イギリスの音楽シーンに影響を受けたスタイルを持つ**。国籍/活動拠点: イギリス」と、逆接語を使わず最初から堂々と虚偽申告。

これは15-2(逆接分割)の対象パターンには当てはまらない、素通りするケース。ただし初回はstatus: skippedで実害なし。

**対策**: `KNOWN_ARTIST_NATIONALITY_OVERRIDES`に`"moses sumney": "usa"`を追加(都度追加の運用通り)。**実装・デプロイ・実運用確認済み**——再テストでjudgeRejectionsに「コード側検証: 既知の国籍補正リストにより指定地域と不一致(自己申告は無視)」としてMoses Sumneyが正しく却下されることを確認した。

### 15-4. バージョン文字列フィルタ(変更7a)の抜け: 単体の"Mix"表記

The Stone Rosesの「Fools Gold - **Grooverider's Mix**」(リミックスアルバム『The Remixes』収録)が選出。`resolveMeta.titleExact: false`かつタイトルが示す通り、意図した原曲ではなくリミックス版だった。既存の`VERSION_MARKERS`正規表現は`remix`/`リミックス`は捕捉するが、単体の"Mix"表記は捕捉できていなかった。

**検討した対策と却下した案**: 単純に`mix`という単語をそのまま追加する案は**誤爆リスクが高いため却下**。理由:
- "Mixed Emotions"のような実在の曲名が誤って弾かれる
- "Original Mix"はダンス/エレクトロニック曲で「オリジナル版」を示す正規のタグ(remixではなくむしろ原曲側の表記)なのに、誤って弾かれてしまう。`grit + texture=100`のようなエレクトロニック系条件はまさにこの表記の曲を集めやすいため、誤爆の実害が大きい

**採用した対策**: 実際に問題になったパターンだけを狙い撃ちする正規表現に絞る。
1. `'s Mix`(所有格 + Mix): 今回の実例「Grooverider's Mix」はこの形。リミキサー名を冠した別バージョンの典型的な命名規則で、実在の曲名に偶然この形が出現する可能性はほぼない
2. `Radio Mix`/`Club Mix`/`Extended Mix`/`Dub Mix`のような、業界で確立された「別バージョン」を示す複合語(既存の`sped up`/`slowed`と同じ、複合語としてのパターンマッチ)

`mix`という裸の単語そのものはリストに追加しない。"Original Mix"はこれらのどのパターンにも一致しないため、安全に除外対象外のままになる設計。

**実装・デプロイ・実運用確認済み(2026-07-19)**: 上記の設計どおり`VERSION_MARKERS`に`['’]s\s+mix|radio\s+mix|club\s+mix|extended\s+mix|dub\s+mix`を追加(直/カーリー両方のアポストロフィに対応)。「Grooverider's Mix」を除外しつつ、「Mixed Emotions」「Original Mix」「Six Mixtapes」等を誤爆しないことを11ケースの回帰テスト(APIを叩かない検証スクリプト)で確認済み。実運用での再テストは未実施。



優先して確認するなら、**`grit + heat=0`が構造的に一番近い(texture問題の対になるテスト)ため最優先**。次いで`grit + india`等の非英米地域カード。全部を先回りして直そうとせず、変更14と同じ運用方針(実際に問題が確認されたものから対応する)を維持する。





### 背景(2026-07-19 追加調査で判明)
9-11(文言追加)実装後も`uk`単体テストで再発(The Strokes=アメリカが選出、candidateWhyに国籍言及なし)。ローカルのコード調査(core.ts, route.ts)で原因が判明:

- `generateCandidates`は**1回のLLM呼び出し**で「候補生成→自己判定→passed/rejected」を全て行う設計。過去に別役の審査パス(LLM二段構成)を試したが、審査を厳しくしすぎて「知ってる曲=定番でNG、知らない曲=不明でNG」のデッドロックを起こした教訓と、却下回数と人間評価が無相関だった実測から、**意図的に別役の審査パスを廃止した経緯がある**(core.tsコメントに明記)。
- つまり「却下と選出が別々の推論」なのではなく、**同じ1回の推論の中で、モデルの自己判定がその回だけ機能しなかった**というのが実体。judgeRejectionsで他の曲を正しく却下していても、最終選出の一貫性は保証されない。
- fill route側(route.ts)には多層フォールバック(L1〜L5)があるが、**passed後のコード側チェックは重複排除・年代ゲート・タイトル解決のみで、国籍/地域の整合性は一度も再検証されない**。resolveTrackはSpotify上でアーティスト名が一致しさえすれば無条件で解決に成功する。
- **既知の別の穴**: L5(種曲フォールバック、fallback.ts)は固定リストからの選出で、**地域カードとの照合が一切ない**。今回のログはsource:"ai"だったためL5は無関係だが、地域偽装の別ルートとして存在する(要フォローアップ、本変更のスコープ外として別途記録)。

結論: プロンプト文言の強化だけでは、モデルの自己判定が外れた場合に検出・修正する手段がコード側に皆無。**コード側にハード検証を追加する**方向に切り替える。

### 対策: candidateWhy/rejectedのテキストから国籍宣言を抽出し、コード側でハード判定する
新しい外部API呼び出しは追加しない(レイテンシほぼゼロ)。モデルが返すテキストをコードで検証する二段構え:

1. **プロンプト側(9-11の延長)**: 地域カード指定時、各候補のcandidateWhyに**必ず**「国籍/活動拠点: ○○」という定型文言を含めることを、より強い表現で義務付ける(「省略不可」「この記載がない場合は不合格として扱われる」等、モデルへの強制力を上げる)。
2. **コード側(新規)**: `generateCandidates`が返したcandidateWhyの文字列から「国籍/活動拠点: ○○」を正規表現等で抽出し、指定されている地域カードの範囲と一致するか機械的にチェックする。一致しない場合、または該当の記載自体が存在しない場合は、その候補を**コード側で強制却下**し、次点候補を採用するか再生成を要求する。

```
【手順】(9-11からの改訂・強い表現に変更)
まず候補を5曲挙げる。次に各候補を、選ばれている性格カード・地域
カードのすべてに照らして自己判定する。地域・言語カードが指定されて
いる場合、各候補について必ず「国籍/活動拠点: ○○」という記載を
含めること(省略不可)。指定地域と一致しない場合、理由を問わず
(知名度・影響力・音楽的つながりの深さを含め)不合格とする。
全カードを満たす曲だけを残し、最も確かな1曲を選ぶ。
```

コード側の検証イメージ(route.tsまたはcore.ts、resolveTrack前後に追加):
```typescript
// 地域カードが指定されている場合、candidateWhyから国籍宣言を抽出しハード検証
function verifyRegionDeclaration(candidateWhy: string, regionCardId: string): boolean {
  const match = candidateWhy.match(/国籍\/活動拠点[:：]\s*(.+?)[。.\n]/);
  if (!match) return false; // 記載なし→不合格
  const declared = match[1];
  return regionMatches(declared, regionCardId); // 地域カードごとの許容国名リストと照合
}
```
`regionMatches`は各地域カード(uk, usa, lang_ja等)に対応する国名・地域名の許容リストを持つ簡易な文字列照合で十分(厳密な国籍データベースは不要)。

### 既知の限界
モデルが国籍宣言自体を書かなかった場合(記載なし)は「不合格」として扱えるため、今回のThe Strokesのケース(国籍言及ゼロ)はこの仕組みで検出可能になる。ただし国籍を誤って(虚偽で)申告した場合は、依然として検出できない——この対策は「書かせて、書いたものを機械的にチェックする」ものであり、モデルの申告内容そのものの真偽までは保証しない。完全な確実性が必要ならMusicBrainz等の外部データソースとの照合(処理コスト増)を別途検討する。

### 再テスト計画
実装後、再び`uk`単体・複数セグメントで再生し、以下を確認する:
- candidateWhyに「国籍/活動拠点: ○○」の記載が毎回あるか(省略されるケースがないか)
- 記載と実際のアーティストの国籍が一致しているか
- 他国アーティストの最終選出が再発していないか(目標: 0件)
- 前回(9セグメント中2件=22%)からの改善幅を記録する

### 既知の限界(モデルの知識誤りは検出不可、2026-07-19実運用で確認)
最新デプロイ(035c381)での実運用テストで、この仕組み自体は設計通りに機能していることを確認した。しかし1件、**モデルの音楽知識そのものの誤りは防げない**ケースが見つかった:

- The Killers(実際はアメリカ・ラスベガスのバンド)が`uk`条件で選出。candidateWhyには指定フォーマット通り「国籍/活動拠点: イギリス」と明記されており、`verifyRegionDeclaration`もこの宣言を正しく抽出し「合格」と判定した(コード側の判定ロジックは正常動作)。
- 原因: The Killersはブリットポップ/ポストパンク・リバイバル的なサウンドを持つため、モデル自身が「これはイギリスのバンドだ」と誤認したとみられる。虚偽申告ではなく、モデルの知識の誤りによる"善意の誤報告"。

これは設計上の想定内の限界であり、追加対応は行わない。「書かせて機械的にチェックする」方式は、モデルが正確な知識を持っている前提の上でのみ機能する。完全な確実性が必要なら外部データソース(MusicBrainz等)との照合が必要になるが、レイテンシ・メンテナンスコストが増えるため、今回は見送る。

なお、今回のテストで判明した重要な事実として、**同日中に観測されていたThe Strokes・Taylor Swift・Tame Impala・Nine Inch Nails・Lana Del Reyの混入は、実は全てコミット・デプロイ漏れにより7/13時点の古いビルドに対して起きていたもの**で、当日実装した9-11/変更12/変更12改訂は一度も本番でテストされていなかったことが判明した(git statusで発覚)。正しいビルドでテストした結果はThe Killers1件のみで、モデル知識誤りによる発生率は当初懸念していたより低い可能性がある。







### 11-1. 局名コール(ジングル)のテキスト短縮
現在の局名コールは「TRIW STATION」を読み上げる固定音声ファイル(public/audio/station-id.mp3、ローカルで生成・固定化済み)になっているが、長く感じるため「STATION」を削除し「TRIW」のみにする。
対応: テキストを変更し、音声ファイルを再生成して差し替える(生成手順は変更8実装時と同じ、既存の使い捨てスクリプトを再利用)。

### 11-2. 時報の合図音とテンポ
時報(現在時刻の読み上げ)について2点:
- 合図の音(サウンドロゴ部分、WebAudioでオシレータ合成している`playJingleIntro`/`playJingleSting`、src/engine/sounds.ts)がピンとこない。音色・鳴らし方を見直す。
- 時刻を伝える話し方が遅すぎる。テンポを上げてきびきびと伝える。

次回着手時にsrc/engine/sounds.tsと時報のテキスト生成・TTS呼び出し箇所(おそらくuseRadioEngine.ts)を確認し、以下を検討する:
- 音の見直しは音色・鳴らす長さ・音数の調整(具体的な好みは実際に鳴らして都度すり合わせる)
- 話速はOpenAI TTS(audio/speech、gpt-4o-mini-tts)のspeedパラメータ(対応していれば0.25〜4.0の範囲で指定可能)を上げる。現状このパラメータを指定しているか確認し、未指定なら明示的に設定する。

### 11-3. ナレーション(曲紹介)の話速
曲間のナレーションも同様に読み上げが遅く感じる。11-2と同じ理由・対処(TTSのspeedパラメータ)で、ナレーション生成のTTS呼び出し箇所にも同じ調整を適用する。速度は時報と揃えるか、ナレーションはやや遅め・時報はきびきび、のように使い分けるかは実際に聴いて判断する。



課題: 曲間のナレーションがつまらない。以下2点を疑っているが、原因は現状の実装(narration生成プロンプトの中身、describeStateとの連携有無)を確認してから切り分ける。

1. **卓の条件を反映していない疑い**: 現在選ばれているカード・スライダー(describeStateの出力)がナレーション生成プロンプトに渡っていない可能性。渡っていれば「なぜ今この曲を選んだか」に触れられるが、渡っていなければ曲名・アーティスト紹介に終始し、平板になりやすい。
2. **言い回しのバリエーション不足の疑い**: 定型文の使い回しになっている可能性。対策としては、紹介の型(曲の背景、選んだ理由、リスナーへの一言、聞きどころ、など)を複数用意して毎回異なる角度から話させる、あるいはプロンプトに「直前と同じ言い回し・切り口を避ける」と明示する。

### 次回着手時の確認事項
```
曲間のナレーション(MC部分)がどう生成されているか教えてください。
生成プロンプトに、現在選ばれているカードやスライダーの条件
(describeStateの出力など)が渡っていますか。渡っているなら
そのプロンプトの中身も教えてください。関連ファイルのパスも
知りたいです。
```
この回答を踏まえて、上記1・2のどちらが原因か切り分けてから改修する。



背景: 変更3(排他)を検討する過程で、「性格を定義するカード(時間・シーン・雰囲気)は互いに椅子取りゲームをしているが、地域・言語カードは単なるフィルタで椅子取りに参加していない」という区別に気づいた。これを踏まえ、カード体系全体を棚卸しした。3枚使い時にgood率が55%→18%に落ちる現象(変更3検討時のログ)は、性格カード同士の交差が過密になることが原因と判断。

### 9-1. 性格カードの選択上限を2枚に固定

時間・シーン・雰囲気カードは合計で**最大2枚まで**。地域・言語カードはこの上限にカウントしない(9-3参照)。

理由: 3枚以上になると条件が過飽和し、モデルは全条件を薄く満たす無難な曲(ok評価)に収束する。2枚までなら「浮いた1枚が黙って無視される」問題(変更6の主因)も構造的に起きなくなる。

**重要(2026-07-14 実装バグ修正の訂正、さらに2026-07-15 再訂正)**: 「性格カード合計2枚まで」は、時間・シーン・気分の3グループを**横断して**合計2枚という意味であり、グループごとに1枚という意味ではない。初回実装では誤って「グループごとに1枚」になっていたため、気分・質感グループから2枚選べない不具合が発生した。正しい仕様:

- 「時間・天気」グループ(morning/midnight/rain/summer)は**グループ内排他**(実質1枚まで。同時に朝と夜更けは選べない、という情景上の制約のため)
- 「シーン」(drive/focus/walk)も**グループ内排他**(実質1枚まで。ドライブ=前に進む推進力、focus=一定の質感が続く集中作業、walk=歩く速度のゆったりした情景は、いずれも同時に成立しにくいため)
- 「気分・質感」(dance/doze/grit/experimental/bittersweet/uplift。nostalgiaは削除が決定済みだがコード側は未実装、9-2参照)には**グループ内排他なし**。同じグループから2枚選んでも構わない(例: dance+uplift、grit+bittersweetは可)。これは初回実装のままで正しかった(一時「グループごとに1枚では」と疑ったが撤回)
- 上記全グループを横断して、選択できる性格カードの**合計は2枚まで**
- 個別の排他ペア(doze×dance、doze×grit、doze×uplift)は上記とは別に維持

### 9-2. 性格カードの棚卸し(20枚→13枚)

較正・議論の結果、以下の通り整理。**削除したカードの情景・感情は、原則として無選択(自由に選んでよい)かheat/textureスライダー、または近縁カードに吸収される設計とする。**

**時間・天気(6→4枚)**
- 残す: `morning`(朝)、`midnight`(夜更け。変更1で情景ベースに書き換え済み)、`rain`(雨、変更なし)
- 削除: `afternoon` — 無選択時のデフォルト("自由に選んでよい")とほぼ等価なため独立カードの必要性が薄い
- 削除: `dusk` — 「感傷的で美しい」という情景がbittersweetと重複。bittersweetの定義に統合(下記9-2追記参照)
- 置換: `sunny_holiday` → `summer`(夏)に差し替え。「晴れた休日」は愛着が薄く、"夏"のほうが強い季節性・文化的磁場を持つため。日本の夏(蝉・花火・シティポップ)に限定せず、アメリカの夏(Summer in the City、Beach Boys的な西海岸/都会の熱気)も含む国・ジャンル非依存の定義にする:
  ```
  summer: "夏の情景。強い日差し、暑さ、開放感——都会のアスファルトの熱気でも、波打ち際の解放感でも、蝉と花火の湿った夏でも、季節としての夏を描いていればよい。国やジャンルを問わず、"夏"という季節感を持つ曲。"
  ```

**シーン(4→2枚、さらに新規1枚追加で3枚)**
- 残す: `drive`(ドライブ、変更なし)、`focus`(作業・集中、変更なし)
- 削除: `kitchen`、`gathering` — 機能面では他と代替不可能ではないが(heat×danceやheat×upliftでかなり再現できる)、判断の決め手はUXだった: driveが"動"、focusが"静"で両極を代表しており、kitchen/gatheringはその中間で存在感が薄い。「一人か人がいるか」という文脈の違いは実在するが、選曲への影響はheatスライダーがかなりの部分を代替する。
- **新規追加: `walk`(散歩)** — シーンカードは地域カードと同じ「一覧から選ぶだけ」の性質を持つため、性格カード全体の2枚上限とは関係なく気楽に増やせる、という判断のもとで追加(2026-07-13決定、指示書への反映漏れを2026-07-19に修正)。driveとの対比が明確: driveは「一定のリズム、前に進む推進力」というスピード感のある移動、walkは歩く速度のゆったりした情景。
  ```
  walk: "歩く速度の情景。急かされない散策、街や公園を眺めながらの時間。
  ドライブほどの推進力はなく、ゆったりと景色や物思いに浸れる曲。"
  ```
  料理(kitchen)、集い(gathering)、運動(workout)、お風呂(bath)、通勤(commute)等、他のシーン候補も枚数を気にせず追加できる状態にあるが、今回はwalkのみ追加とし、他は「欲しくなったら追加する」保留とした。

**気分・質感(10→7枚)**
- 残す: `dance`、`doze`、`grit`(変更2で境界拡張済み)、`experimental`(いずれも変更なし、他と重複しない固有の軸を持つと判断)
- 残す: `uplift` — 一度「heat×danceで代替可能では」と疑ったが、upliftが指定するのは"曲の中の起伏の構造"(ビルドアップして解放される)であり、heat(感情の温度)ともdance(リズム)とも独立した軸と判明。EDM的な高揚感はheat×danceだけでは出ない(heatのホット端は「熱唱・汗」という生々しい肉体性の軸で、EDMのひんやりした多幸感とは逆方向)。撤回して残留。
- 残す: `bittersweet` — duskの「昼と夜の境目の感傷」を吸収する形で定義を拡張:
  ```
  bittersweet: "甘さと痛みが同居する感情。マイナーとメジャーの揺らぎ、郷愁を誘うメロディ。夕暮れのような、昼と夜の境目の感傷も含む。"
  ```
- 決定(未実装。現在地の未着手リスト参照): `nostalgia` — bittersweetとの役割重複が実運用で判明(「郷愁を誘うメロディ」と「世代の記憶に触れる」がほぼ同じ意味になっていた)。年代スライダー(era)と連動させる案(「eraで指定した年代の当時の音らしさ」)は、era設定によってはeraの言い換えになってしまい自己矛盾。「実際のリリース年に関わらず意図的に懐古を演じている曲(シティポップ・リバイバル等)」という独立軸としての再定義案も検討したが、日常使いにはニッチすぎると判断し削除で確定した(2026-07-15決定)。感情としての郷愁はbittersweetでカバーする。**ただし2026-07-19時点でまだコード側の削除は未実施**——実際のログでもnostalgiaカードは引き続き選択可能な状態で動いている。次回対応時に実装すること。
- 削除: `uplift`系と誤読しないよう明記——削除したのは `romance`、`nature`、`city` の3枚:
  - `romance`: 「甘く親密」はbittersweet寄りの感情+heat低〜中で代替可能。ジャンル指定(ソウル、ボサノバ)に寄りすぎていた定義の粗さも要因。
  - `nature`: 「アコースティックな手触り」はtextureスライダー(9-4で新設)のアコースティック端とほぼ等価。
  - `city`: 「シティポップ、ネオソウル」はジャンル指定に近く、むしろ地域・都市文化の指定に近い。heat低め×textureエレクトロニック寄りでかなり再現可能。ただし「Take Fiveのようなジャズが出したいときの受け皿」を失う副作用があり、これがtextureスライダー新設の直接のきっかけになった。

**保留(次の季節が来たら追加)**
- `christmas`(クリスマス): 夏とは性質が異なる「特定の日付に紐づく祝祭」カード。常設ではなく季節限定で追加する運用とし、今回のスコープには含めない。

### 9-3. 地域・言語カードの拡充(7→12枚)、選択ルールを1枚のみに変更

**選択ルール変更**: 地域・言語カードは**全体で1枚のみ**選択可(現行の「複数指定時は交互に」という仕様は削除)。理由: 地域カードはフィルタとして機能し、性格カードのような組み合わせの妙味が薄い。2つの地域を同時に聴きたい需要も薄いと判断。実装がシンプルになる副次効果もある。

**新規追加(5枚)**:
```typescript
{
  id: "india",
  label: "インド",
  promptText: "インドのアーティストの曲だけを選ぶ。ボリウッド、インド古典(ヒンドゥスターニー/カルナータカ)、現代インディー、バングラ、スーフィー音楽まで幅広く。",
},
{
  id: "middle_east",
  label: "中東",
  promptText: "中東(アラビア語圏、ペルシャ語圏、トルコ、イスラエルなど)のアーティストの曲だけを選ぶ。伝統的なマカーム音楽から現代のインディー・ポップまで。",
},
{
  id: "caribbean",
  label: "カリブ海",
  promptText: "カリブ海地域(ジャマイカ、トリニダード、ハイチなど英語・フランス語圏)のアーティストの曲だけを選ぶ。レゲエ、ダンスホール、ソカ、カリプソ、ズークなど。",
},
{
  id: "uk",
  label: "イギリス",
  promptText: "イギリスのアーティストの曲だけを選ぶ。ブリットポップ、UKガラージ、グライム、北部ソウル、フォークリバイバルなど、島国ならではの内向きな進化を持つ音楽。",
},
{
  id: "usa",
  label: "アメリカ",
  promptText: "アメリカのアーティストの曲だけを選ぶ。ソウル、ヒップホップ、カントリー、ブルース、インディーロックなど地域ごとに枝分かれした大陸規模の音楽伝統。",
},
```

**既存カードの定義文調整**:
- `asia`: 例示からインドを削除し、東南アジア・東アジア(日韓以外)に絞る(「台湾、香港、タイ、インドネシア、フィリピン、ベトナムなど」インドは`india`に独立)
- `europe`: 「(イギリスを除く)」を明記し、UKと役割が重複しないようにする

### 9-4. スライダーにtextureを新設(3→4本)

cityとnatureを削除した結果、「アコースティック⇄エレクトロニック」という軸が消失していたことに気づいた(city選択時にTake Fiveのようなアコースティック・ジャズが頻出していた実績あり)。この軸はheat(感情の温度)ともera(年代)とも直交するため、新規スライダーとして独立させる。

```typescript
{
  id: "texture",
  label: "質感",
  leftLabel: "アコースティック",
  rightLabel: "エレクトロニック",
  defaultValue: 50,
  bands: [
    { min: 0, max: 19, label: "アコースティック", promptText: "生楽器中心。ギター、ピアノ、弦、生ドラムなど、電子的な加工が最小限の音。シンセ・打ち込み・エレクトロニックプロダクションは不可。" },
    { min: 20, max: 39, label: "ややアコースティック", promptText: "生楽器を中心にしつつ、控えめな電子的処理を許容。" },
    { min: 40, max: 60, label: "指定なし", promptText: "質感は自由。" },
    { min: 61, max: 80, label: "ややエレクトロニック", promptText: "シンセや打ち込みを中心にしつつ、生楽器の要素も許容。" },
    { min: 81, max: 100, label: "エレクトロニック", promptText: "シンセ・打ち込み・電子的プロダクションが主体の曲。生楽器主体のアコースティックな演奏は不可。" },
  ],
},
```

`describeState` の `sliderLines` は既存の `sliders.map(...)` 実装のままで自動的にtextureも出力される(配列に追加するだけでよい)。

### 9-4-補足. popularityスライダーは変更なし(検討したが見送り)

popularityの「深掘り⇄定番」という軸に「ピンとこない」という声があり、代替として「陽キャ⇄陰キャ」のような軸への差し替えを検討した。検討の結果、以下の理由で**見送り、現状維持**とした:

- 陽キャ的な曲はheatのホット端+dance+upliftで、陰キャ的な曲はheatのクール端+doze+bittersweet+grit(ローファイ)で、既存軸の組み合わせによってかなりの部分が代替できてしまう(重複)
- popularityは実際の知名度という外部で検証可能な基準を持つが、陽キャ/陰キャは純粋にモデルの主観的なムード判定であり、検証しようがない(ハルシネーションの温床になりやすい)

popularityスライダーはこのままの定義(深掘り⇄定番)で変更しない。

### 9-5. 変更3(排他)の更新

変更3で示した「時間帯グループ排他」は、9-1の「性格カード合計2枚まで」という上限によって実質的に上位互換されるため、`exclusionGroups`(時間帯排他)は不要になる可能性が高い。ただし、`exclusionPairs`(doze×dance、doze×grit、doze×uplift)は性格の組み合わせとして依然矛盾するため維持する。

```typescript
// 選択上限(UIで強制)
export const MAX_PERSONALITY_CARDS = 2; // 時間・シーン・雰囲気カードの合計
export const MAX_REGION_CARDS = 1; // 地域・言語カードの合計

// 排他ペア(性格カード内で物理的に両立しない組み合わせ)
export const exclusionPairs: [string, string][] = [
  ["doze", "dance"],
  ["doze", "grit"],
  ["doze", "uplift"],
];
```

### 9-6. カード体系の最終形(サマリ)

- **性格カード**: 13枚(時間・天気4 + シーン3 + 気分・質感6)、選択上限2枚(うち時間・天気とシーンはグループ内排他で実質各1枚まで)
  - morning, midnight, rain, summer, drive, focus, walk, dance, doze, grit, experimental, bittersweet, uplift
  - (nostalgiaは削除が決定済みだがコード側は未実装、9-2参照。walkは2026-07-13決定・2026-07-19に指示書へ反映、こちらもコード側は未実装)
- **地域・言語カード**: 12枚、選択上限1枚
  - lang_ja, kr, asia, latin, africa, europe, world_trip, india, middle_east, caribbean, uk, usa
- **スライダー**: 4本(era, heat, popularity, texture)
- **保留**: christmas(季節限定枠、12月に追加検討)

### 9-7. 再テスト計画の更新

変更9によりカード構成が変わったため、当初(冒頭)の再テスト計画にあった `dusk + nostalgia + grit` は存在しないカードを含む。再テストは以下に差し替える:

1. `midnight + lang_ja`(era=0, popularity=100)は引き続き有効な再テスト対象。
2. 3枚組の再テストは不要になる(性格カード上限が2枚のため、3枚組自体が存在しなくなる)。代わりに、2枚の性格カード+1枚の地域カードという新しい標準形(例: `midnight + grit + uk`)でbad率を確認する。
3. 新設カード(summer、texture)の初回テストを追加する: `summer + dance`、`texture(アコースティック端) + focus` など。
4. 新設地域カード(india、middle_east、caribbean、uk、usa)は、既存の英語圏一括りだった選曲がどう変わるかを確認する意味でも優先的にテストする価値がある。

### 9-10. 新規発見: lang_ja + dance がアイドル歌謡に偏る(2026-07-14 実運用フィードバック)

デプロイ後の実聴で判明。`lang_ja + dance` の組み合わせで、AAA・AKB48のような量産的なアイドル歌謡ばかりが出てくる。原因は`dance`のpromptText内の「ダンサブルなポップ/R&Bなど」という広い例示が、日本語という制約と掛け合わさったときに一番手近などまん中の解釈(アイドルポップ)へモデルを吸い寄せているため。gritで起きた「例示の重力」と同種の問題。

本来期待される選曲: 和モノファンク、渋谷系のクラブ寄り(Pizzicato Fiveなど)、シティポップ・リバイバル系のダンサブルな曲、90年代クラブジャズ/渋谷系ハウスなど。アイドル歌謡はこのプールのごく一部でしかない。

対処方針(未実装、次回のタスク): dance自体の定義は他言語・地域では機能している可能性が高いため触らず、comboHintsに `["lang_ja", "dance"]` を追加する形で対処する。

```typescript
{
  cards: ["lang_ja", "dance"],
  promptText: "日本語の踊れる曲。和モノファンク、渋谷系のクラブ・ハウス、シティポップ・リバイバルのダンサブルな曲調など。量産的なアイドルポップのダンスナンバーは避ける。",
},
```

再テスト時に `lang_ja + dance` を追加すること(9-7の再テスト計画に追記)。

参考: `lang_ja + grit`(+ textureをアコースティック寄り)は実運用で良好(THE PILLOWSなど、狙い通りの日本語オルタナ/バンドサウンドが出た実績あり)。こちらはcomboHint不要と判断。textureとの組み合わせ次第で結果が変わる可能性があるため、texture指定なしの場合も改めて確認する価値あり。

変更6の骨格を、9-1(性格カード最大2枚+地域1枚)を前提に整理し直した完成版。カード数の上限がUI側で強制されるため、「全カード合格」の判定はほぼ常に2〜3枚程度で済み、変更6で懸念していた過負荷は起きない想定。

```
あなたはラジオDJとして次の曲を選ぶ。

【いまの卓の条件】
{describeState() の出力}
{comboHints 該当時は【組み合わせの解釈】}

【選曲の原則】
1. 雰囲気カードは「情景・感情」の指定であり、ジャンルの指定ではない。
   条件を満たすなら、どのジャンル・国・年代からでも選んでよい。
   定義文の例示はジャンルの範囲ではなく、軸がジャンルを横断することを
   示す見本である。
2. 条件が両立しないときは【条件が両立しないとき】の優先順位に従う。
3. 直近の流れと同じアーティスト・同じ質感が続いたら、条件の範囲内で
   意図的に離れた場所から選ぶ。

【判定の較正例】
「静かな曲」と「夜更けの曲」は別物である。風をあつめて(はっぴいえんど)は
静かだが昼の情景であり midnight に不適。ブルー・ライト・ヨコハマは静かでは
ないが夜の情景であり適合。判定は音の静かさではなく情景で行う。

【手順】
まず候補を5曲挙げる。次に各候補を、選ばれている性格カード(最大2枚)・
地域カード(最大1枚)のすべてに照らして自己判定する——その曲が実際に
持つ情景・性格に基づいて。全カードを満たす曲だけを残す。
「合うと言えなくもない」程度の曲は落とす。よく知らない曲は、落とさずに
理由へ「推測」と明記して残してよい(無名曲は発見の源泉のため排除しない)。
最も確かな1曲を選び、実際の特徴だけを理由として出力する。
```




