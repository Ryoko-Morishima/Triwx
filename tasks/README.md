# tasks/ — タスクの進め方

- 各ファイルが1エージェント(1セッション)で引き継げる作業単位
- 着手前に PROJECT.md → AGENTS.md → HANDOFF.md → 対象タスクを読む
- 着手時にステータスを in-progress、完了時に done に更新
- **中断時は「中断時の引き継ぎ欄」に現在地と次の一手を必ず残す**（引き継ぎの生命線）
- ステータス: todo / in-progress / blocked(理由必須) / done
- スコープ外に気づいたら新タスクを起票する（混ぜない）
